## 💻 خطوات تنزيل المشروع وتشغيله

1. **استنساخ المشروع من GitHub**
   ```bash
   git clone <رابط_المشروع>
   cd complaints-system

تثبيت المكتبات

composer install


إنشاء نسخة من ملف البيئة

cp .env.example .env


توليد مفتاح التطبيق

php artisan key:generate

🛠️ إعداد ملف .env

افتح ملف .env وعدّل القيم التالية حسب إعدادات جهازك:
ولا تنسى هدول مشان ال otp: 
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=almgdlawiansam@gmail.com
MAIL_PASSWORD="waxk mscf qqce orto"
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=almgdlawiansam@gmail.com
MAIL_FROM_NAME="نظام البنك"
++++++++++++++مشان الادمن:
SYSTEM_ADMIN_EMAIL=admin@bank.local
SYSTEM_ADMIN_PASSWORD=Admin@12345
///////////////////////////////////////////
تشغيل الـ Migrations والـ Seeders

لتجهيز قاعدة البيانات من الصفر:

php artisan migrate:fresh --seed


سيقوم هذا الأمر بإنشاء جميع الجداول وإضافة البيانات الأساسية (مثل الجهات الحكومية وحساب الأدمن).



مهم للموبايل:
نظام OTP والتحقق

عند تسجيل الدخول لأول مرة، يتم إرسال رمز تحقق (OTP) إلى البريد الإلكتروني.
يجب التنبيه إلى ما يلي:

زر إعادة إرسال OTP يجب أن يكون غير قابل للضغط لمدة دقيقتين بعد الإرسال.

السبب: منع المستخدم من إرسال طلبات متكررة للبريد مما قد يؤدي إلى حظر الحساب أو تعطيل السيرفر.


كل الشكاوى يتم حفظها مع رقم مرجعي فريد (UUID).

المرفقات تُخزّن داخل المجلد:

public/storage/complaint_attachments


للوصول إلى المرفقات عبر API:

GET /api/attachments/{id}


Endpoint لجلب أنواع الشكاوى:

GET /api/complaint-types


Endpoint لجلب جميع الجهات الحكومية:

GET /api/government-agencies

تأكد من تشغيل أمر الربط بين storage و public قبل رفع الملفات:

php artisan storage:link


ابعتولي ايميلاتكن مشان دعوة البوستمان